name: Deploying
on:
  push:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Debug Secrets
        run: |
          echo "FTP_SERVER starts with: ${FTP_SERVER:0:3}***"
          echo "SERVER_DIR starts with: ${SERVER_DIR:0:3}***"
          echo "SSH_HOST starts with: ${SSH_HOST:0:3}***"
          echo "SSH_PORT is set: ${{ secrets.SSH_PORT != '' }}"
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          SERVER_DIR: ${{ secrets.SERVER_DIR }}
          SSH_HOST: ${{ secrets.SSH_HOST }}
          
      # - name: Deploy to cpanel
      #   uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      #   with:
      #     server: ${{ secrets.FTP_SERVER }}
      #     username: ${{ secrets.FTP_USERNAME }}
      #     password: ${{ secrets.FTP_PASSWORD }}
      #     local-dir: ./
      #     server-dir: ${{ secrets.SERVER_DIR }}
      #     exclude: |
      #       **/.git*
      #       **/.git*/**
      #       **/node_modules/**
      #       .env.example
            
      # - name: Run post-deployment commands via SSH
      #   uses: appleboy/ssh-action@v1.2.1
      #   with:
      #     host: ${{ secrets.SSH_HOST }}
      #     username: ${{ secrets.SSH_USERNAME }}
      #     password: ${{ secrets.SSH_PASSWORD }}
      #     key: ${{ secrets.SSH_KEY }}
      #     port: ${{ secrets.SSH_PORT }}
      #     script: |
      #       cd ${{ secrets.SERVER_DIR }}
      #       php artisan migrate --force
      #       php artisan optimize
      #       php artisan cache:clear
      #       php artisan config:cache
      #       php artisan route:cache
      #       php artisan view:cache
